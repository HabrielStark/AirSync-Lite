# AirSync-Lite Environment Configuration
# Copy to .env and customize for your environment

# Application Mode (development | production)
NODE_ENV=development

# Network Configuration
SYNC_PORT=40777
LOG_LEVEL=info

# Auto-update settings (leave empty for defaults)
AUTO_UPDATE_URL=

# Proxy settings (optional)
HTTPS_PROXY=
NO_PROXY=localhost,127.0.0.1

# Development only (DO NOT enable in production)
ENABLE_DEVTOOLS=false

# Security (NEVER commit actual secrets)
# Store secrets in OS keychain via the app's keychain manager instead
# STRIPE_SECRET_KEY=sk_test_... (use keychain instead)

# CI/CD Environment
# These are set automatically in GitHub Actions
# GITHUB_TOKEN=
# GITHUB_SHA=
# GITHUB_REF=
